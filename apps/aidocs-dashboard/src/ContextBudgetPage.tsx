export function ContextBudgetPage() {
  return (
    <section className="page">
      <section className="flat-panel">
        <div className="section-label">Context Budget</div>
        <p>Context budget monitoring is available on the Overview page. Use the compact button there to prune journal entries and reset token counters when the context window fills up.</p>
      </section>
    </section>
  );
}
