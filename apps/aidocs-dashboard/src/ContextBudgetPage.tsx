export function ContextBudgetPage() {
  return (
    <section className="page">
      <section className="flat-panel">
        <div className="section-label">Context Budget</div>
        <p>Backend context budget tools are not yet available in this repo. The widget is intentionally not wired until a real budget/check + compact backend exists.</p>
      </section>
    </section>
  );
}
