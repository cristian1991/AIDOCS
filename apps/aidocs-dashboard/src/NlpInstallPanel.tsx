// Deprecated 2026-04-23: NLP stack (lingua+simplemma+rapidfuzz) ships
// by default. Dashboard install button removed. File kept as a stub so
// stale imports surface a clear error instead of a silent 404.
export function NlpInstallSection(): null {
  return null;
}

